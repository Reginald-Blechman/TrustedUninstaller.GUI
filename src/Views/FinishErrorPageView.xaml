<UserControl x:Class="TrustedUninstaller.GUI.Views.FinishErrorPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrustedUninstaller.GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="70" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Process Completed" Style="{DynamicResource BlueHeaderText}" FontSize="16" />
            <Grid Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image Name="VersionIcon" Source="/Icons/playbook_frame_128.png" Width="24" Margin="5,5,5,5" />
                <TextBlock Name="RebootText" VerticalAlignment="Center" Grid.Column="1" Text="The Playbook completed with notable errors. Contact the team for help." Style="{DynamicResource InText}" />
            </Grid>
            <Grid Row="2">
                <TextBox Name="errorBox" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Style="{DynamicResource Output}" Padding="3,0,0,5" Margin="5,15,5,-233" VerticalAlignment="Top" VerticalContentAlignment="Bottom" />
            </Grid>
        </Grid>
        <TextBlock VerticalAlignment="Bottom" Margin="10,0,-10,10" Grid.Row="1" Style="{DynamicResource InText}">Click the <Bold>Reboot</Bold> button below to finish the setup.</TextBlock>
    </Grid>
</UserControl>
