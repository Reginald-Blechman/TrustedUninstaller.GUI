<Page x:Class="TrustedUninstaller.GUI.Views.ProgressPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TrustedUninstaller.GUI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProgressPageView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="0*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Amelioration in progress" Style="{DynamicResource Header}" Margin="0,0,0,15" />
            <TextBlock Style="{DynamicResource InText}" Text="This process may take a while to complete." />
        </StackPanel>
        <ProgressBar Name="InstallationProgressBar" Margin="0,18,0,-35" Grid.Row="1" Foreground="#FF0078D6" Value="{Binding Progress}" />
        <TextBox Name="textBox" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Style="{DynamicResource Output}" Padding="3,0,0,5" Margin="0,58,0,-290" Grid.Row="1" />
    </Grid>
</Page>
