<UserControl x:Class="TrustedUninstaller.GUI.Views.FinishPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrustedUninstaller.GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="70" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Process Completed" Style="{DynamicResource BlueHeaderText}" FontSize="16" />
            <Grid Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image Name="VersionIcon" Source="/Icons/green_check_symbol.png" Width="24" Margin="5,5,5,5" />
                <TextBlock Name="RebootText" VerticalAlignment="Center" Grid.Column="1" Text="The Playbook completed successfully." Style="{DynamicResource InText}" />
            </Grid>
        </Grid>
        <TextBlock VerticalAlignment="Bottom" Margin="10,0,-10,10" Grid.Row="1" Style="{DynamicResource InText}">Click the <Bold>Reboot</Bold> button below to finish the setup.</TextBlock>
    </Grid>
</UserControl>
