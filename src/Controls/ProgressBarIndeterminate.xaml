<UserControl x:Class="TrustedUninstaller.GUI.Controls.ProgressBarIndeterminate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrustedUninstaller.GUI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Name="Container" Background="#00FFFFFF" Height="{Binding BarHeight}">
        <Border Name="Rect" HorizontalAlignment="Left" BorderThickness="{Binding RectBorderThickness}" BorderBrush="{Binding RectBorderBrush}" Background="{Binding RectFill}" Height="{Binding BarHeight}" Width="{Binding RectWidth}" CornerRadius="{Binding RectCornerRadius}" />
    </Border>
</UserControl>
