<controls:AcrylicWindow x:Class="TrustedUninstaller.GUI.Windows.SecurityDialog"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:controls="clr-namespace:TrustedUninstaller.GUI.Controls"
                            xmlns:gui="clr-namespace:TrustedUninstaller.GUI" Height="584" Width="855"
                            Style="{DynamicResource Window}"
                            WindowStartupLocation="CenterOwner"
                            RenderTransformOrigin="0.5,0.5" 
                            Title="Disable Security"
                            CornerType="Round" 
                            MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}" 
                            MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}">

    <UIElement.RenderTransform>
        <TransformGroup>
            <ScaleTransform CenterX="100" x:Name="securityscale" />
        </TransformGroup>
    </UIElement.RenderTransform>
    <Grid Name="RootWindow">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Rectangle Grid.RowSpan="2" Fill="{DynamicResource PrimaryBrush}" Margin="0,0,0,0">
            <UIElement.Clip>
                <CombinedGeometry GeometryCombineMode="Exclude">
                    <CombinedGeometry.Geometry1>
                        <RectangleGeometry Rect="0,0,1000,1000" />
                    </CombinedGeometry.Geometry1>
                    <CombinedGeometry.Geometry2>
                        <RectangleGeometry RadiusX="8" RadiusY="8" Rect="395.5,40.5,1000,500" />
                    </CombinedGeometry.Geometry2>
                </CombinedGeometry>
            </UIElement.Clip>
        </Rectangle>
        <DockPanel Name="TitleBar" Dock="Top" Height="40" VerticalAlignment="Top">
            <Image Style="{StaticResource QualityImage}" Margin="8, 6,0,0" VerticalAlignment="Top" Source="/Icons/ICO.ico" Width="21" Height="21" />
            <Button Name="CloseButton" Style="{DynamicResource CloseButton}" Padding="0,0,0,50" Visibility="Visible" IsEnabled="True" DockPanel.Dock="Right" VerticalAlignment="Top" Click="CloseButton_Click" />
            <TextBlock Name="TitleSpace" VerticalAlignment="Top" Margin="7,0,0,0" Padding="0,11,0,9" FontSize="12" LineStackingStrategy="BlockLineHeight" LineHeight="12" Width="770" Text="Disable Security" Height="25" MouseLeftButtonDown="TitleBar_MouseDown" />
        </DockPanel>
        <Grid Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="395" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid VerticalAlignment="Top" Margin="0,51,0,0" UseLayoutRounding="True">
                <Image Name="SecurityImage" Source="{DynamicResource WindowsSecurityBitmap}" Style="{DynamicResource QualityImage}" Height="399" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <UIElement.Effect>
                        <DropShadowEffect BlurRadius="18" Opacity="0.45" ShadowDepth="0" />
                    </UIElement.Effect>
                </Image>
                <Image Name="RealtimeImg" Style="{DynamicResource QualityImage}" Margin="80,75,0,0" Height="11" Source="{DynamicResource SecurityToggleOnBitmap}" VerticalAlignment="Top" HorizontalAlignment="Left" />
                <Image Name="CloudImg" Style="{DynamicResource QualityImage}" Margin="80,170,0,0" Height="11" Source="{DynamicResource SecurityToggleOffBitmap}" VerticalAlignment="Top" HorizontalAlignment="Left" />
                <Image Name="SampleImg" Style="{DynamicResource QualityImage}" Margin="80,266,0,0" Height="11" Source="{DynamicResource SecurityToggleOnBitmap}" VerticalAlignment="Top" HorizontalAlignment="Left" />
                <Image Name="TamperImg" Style="{DynamicResource QualityImage}" Margin="80,361,0,0" Height="11" Source="{DynamicResource SecurityToggleOnBitmap}" VerticalAlignment="Top" HorizontalAlignment="Left" />
            </Grid>
            <Grid Column="1" Height="501" VerticalAlignment="Top">
                <Border Background="{DynamicResource PageBackground}" CornerRadius="9,0,0,9" />
                <Border Name="PageContainer" BorderBrush="{DynamicResource PageBorderBrush}" BorderThickness="1,1,0,1" Background="#00FFFFFF" CornerRadius="8,0,0,8" Padding="29,31,29,0">
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                        <TextBlock FontSize="16" FontWeight="Bold" Text="Disable Windows Security" />
                        <TextBlock Name="ToStartText" FontSize="13" TextWrapping="Wrap" Margin="0,14,0,22" />
                        <Grid Name="ModuleGrid" Margin="0,-96,0,0" Opacity="0">
                            <TextBlock />
                            <StackPanel Name="PolicyGrid" Visibility="Hidden">
                                <UIElement.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform CenterX="100" x:Name="policiestransform" />
                                    </TransformGroup>
                                </UIElement.RenderTransform>
                                <controls:TextListItem x:Name="ResetText" Prefix="1.     " PrefixVisibility="Hidden" FontSize="13" MaxWidth="380" HorizontalAlignment="Left">
                                    <controls:TextListItem.Inlines>
                                        <Run FontSize="13" Text="Policies that govern Windows Security toggles have been&#xD;&#xA;detected. These " />
                                        <Run FontSize="13" FontWeight="Bold" Text="need to be reset" />
                                        <Run FontSize="13" Text=" to default." />
                                    </controls:TextListItem.Inlines>
                                </controls:TextListItem>
                                <TextBlock />
                                <StackPanel Margin="5,-1,0,0" Orientation="Horizontal">
                                    <TextBlock Text="       " />
                                    <Button Name="ErasePolicies" Width="172" Style="{DynamicResource ContentBoxButton}" Click="ErasePolicies_OnClick">
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Margin="0,0,0,1" Text="Reset security policies" />
                                    </Button>
                                    <Grid Name="LoadContainer" Margin="8,0,0,0" Visibility="Collapsed" Width="16" Height="16" />
                                </StackPanel>
                                <TextBlock />
                            </StackPanel>
                            <StackPanel Name="HelpGrid" Visibility="Hidden" RenderTransformOrigin="0.5,0.5">
                                <UIElement.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform CenterX="100" x:Name="helptransform" />
                                    </TransformGroup>
                                </UIElement.RenderTransform>
                                <controls:TextListItem Prefix="1.     " PrefixVisibility="Hidden" FontSize="13" MaxWidth="380" HorizontalAlignment="Left">
                                    <controls:TextListItem.Inlines>
                                        <Run FontSize="13" Text="Having trouble setting the toggles? You can follow our guide&#xD;&#xA;with some tips, or " />
                                        <Run FontSize="13" FontWeight="Bold" Text="manually disable" />
                                        <Run FontSize="13" Text=" Windows Security." />
                                    </controls:TextListItem.Inlines>
                                </controls:TextListItem>
                                <TextBlock />
                                <StackPanel Margin="5,-1,0,0" Orientation="Horizontal">
                                    <TextBlock Text="       " />
                                    <Button Name="HelpButton" Width="172" Style="{DynamicResource ContentBoxButton}" Click="HelpButton_OnClick">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                                            <Image Height="14" Style="{StaticResource QualityImage}" Source="{DynamicResource ExternalLink32}" Margin="0,0,11,0" />
                                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Margin="0,0,0,1" Text="Open external link" />
                                        </StackPanel>
                                    </Button>
                                    <Button Name="ImGoodButton" Height="32" Margin="8,0,0,-6" Padding="11,4,11,4" Style="{DynamicResource TransparentButton}" Click="ImGoodButton_OnClick">
                                        <TextBlock Name="ImGoodText" VerticalAlignment="Center" FontSize="14" Margin="0,0,0,1" Text="No, I'm good" Foreground="{DynamicResource TransparentButtonTextForeground}" />
                                    </Button>
                                </StackPanel>
                                <TextBlock />
                            </StackPanel>
                        </Grid>
                        <StackPanel Name="Steps">
                            <controls:TextListItem Margin="0,9,0,0" Prefix="1.     " FontSize="13" TextWrapping="Wrap" MaxWidth="380" HorizontalAlignment="Left">
                                <controls:TextListItem.Inlines>
                                    <Run FontSize="13" Text="Click the button below or open " />
                                    <Run FontSize="13" FontWeight="Bold" Text="Windows Security " />
                                    <Run FontWeight="Bold" FontSize="8" Text="▶" BaselineAlignment="Center" />
                                    <Run FontSize="13" FontWeight="Bold" Text="&#xD;&#xA;Virus &amp; threat protection" />
                                    <Run FontSize="13" Text=", and then click " />
                                    <Run FontSize="13" FontWeight="Bold" Text="manage settings" />
                                    <Run FontSize="13" Text="." />
                                </controls:TextListItem.Inlines>
                            </controls:TextListItem>
                            <TextBlock />
                            <StackPanel Margin="5,-1,0,0" Orientation="Horizontal">
                                <TextBlock Text="       " />
                                <Button Name="OpenSecurityButton" Style="{DynamicResource ContentBoxButton}" Click="OpenSecurity">
                                    <TextBlock VerticalAlignment="Center" FontSize="14" Margin="0,0,0,1" Text="Open Windows Security" />
                                </Button>
                            </StackPanel>
                            <TextBlock />
                            <controls:TextListItem Margin="0,9,0,0" FontSize="13" Prefix="2.     " TextWrapping="Wrap">
                                <controls:TextListItem.Inlines>
                                    <Run FontSize="13" Text="Set all four security toggles to off." />
                                </controls:TextListItem.Inlines>
                            </controls:TextListItem>
                            <Grid Margin="3,53,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid Width="49" Height="49" HorizontalAlignment="Left">
                                    <Image Style="{DynamicResource QualityImage}" Source="/Icons/shield_blue_gradient_64.png" Width="44" Height="47" HorizontalAlignment="Left" VerticalAlignment="Top" />
                                    <Image Name="CheckMark" Style="{DynamicResource QualityImage}" Source="/Icons/checkmark_green_gradient_32.png" Visibility="Visible" Height="21" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                                </Grid>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Left">
                                    <TextBlock Name="StatusText" FontWeight="Bold" Margin="0,4,0,3" FontSize="15" Text="Checking Windows Security..." />
                                    <controls:ProgressBarIndeterminate x:Name="ProgressBar" Width="292" HorizontalAlignment="Left" />
                                    <TextBlock Name="FinishText" Visibility="Collapsed" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,-1,0,0" Text="This window can now be closed" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</controls:AcrylicWindow>